/** The connector's own version, reported at pairing and on every /capabilities call — the engine gates flows on it (node_connector_update_required precedent). Keep in lockstep with package.json. */
export const CONNECTOR_VERSION = '0.4.1';
